package jss;

import java.util.ArrayList;
import java.util.List;

/**
 * Multiple problem instances.
 * @author parkjohn
 *
 */
public class ProblemDataset {

	List<ProblemInstance> problemInstances = new ArrayList<ProblemInstance>();

	// TODO so what does this guy need?
	// - factory (obviously) to generate the dataset.
	// -
	public ProblemDataset(ProblemInstanceFactory factory) {

	}
}
